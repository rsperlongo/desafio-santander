export interface Atividades {
    cdUn: string;
    nmAg: string;
    parciais: number;
    concluidas: number;
    periodo: number;
    percentual: number;
}

export interface Abertura {
    cdUn: string,
    nmAg: string,
    parciais: number,
    concluidas: number,
    periodo: number,
    percentual: number,
}